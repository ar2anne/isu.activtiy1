import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'isu.activtiy1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
